/** Placeholder — el login real (POST /v1/auth/login) llega en FE-02. */
export function LoginPage() {
  return (
    <div className="h-full flex flex-col items-center justify-center bg-slate-50 px-4">
      <div className="w-full max-w-[440px] bg-white border border-slate-200 shadow-sm rounded-sm p-8 text-center">
        <h1 className="text-2xl font-bold text-slate-900">Iniciar sesión</h1>
        <p className="mt-2 text-sm text-slate-500">En construcción (FE-02)</p>
      </div>
    </div>
  );
}
